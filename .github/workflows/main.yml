name: Run Python Script

on:
  # Run on every push to the 'main' branch
  push:
    branches: [main]
  # Run automatically on a schedule (e.g., every day at midnight UTC)
  schedule:
    - cron: '0 0 * * *'
  # Allows you to manually trigger the workflow from the Actions tab
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # 1. Access the files in your repository
      - name: Checkout repository
        uses: [actions/checkout@v4](https://github.com)

      # 2. Set up the specific Python version
      - name: Set up Python
        uses: [actions/setup-python@v5](https://github.com/actions/setup-python)
        with:
          python-version: '3.10'

      # 3. Install your project's dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      # 4. Execute your Python file
      - name: Run script
        run: python stock_analysis.py
